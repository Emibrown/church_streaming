<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

<script>
    function clean(obj) {
        for (var prop in obj) { 
            if (obj[prop] === null || obj[prop] === "") {
                delete obj[prop];
            }
        }
    } 
    function displaymsg(text){
        swal("Success!", text, "success");     
    }
    function displayerr(text){
        swal("Fail!", text, "warning");
    }
</script>